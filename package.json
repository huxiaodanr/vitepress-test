{
  "scripts": {
    "docs:dev": "ENV=qa vitepress dev docs",
    "docs:build": "ENV=qa vitepress build docs && ENV=dev vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.0.0-rc.34"
  }
}
